apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-subchart.fullname" . }}-test-connection"
  labels:
    {{- include "my-subchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-subchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
